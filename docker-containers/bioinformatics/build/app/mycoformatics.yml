name: mycoformatics
channels:
  - nodefaults
  - bioconda
  - conda-forge
dependencies:
  - bioconda::mothur  # bioconda (here to conda-forge comment)
  - bioconda::vsearch
  - bioconda::blast
  - bioconda::itsx
  - bioconda::seqkit
  - bioconda::cutadapt
  - bioconda::lima  # might take out, if no one but me will use
  - bioconda::bbmap
  - bioconda::rdp_classifier
  - bioconda::amptk  # will not work with strict channel priority
  - conda-forge::tomlkit  # conda-forge (here down)
  - conda-forge::pip